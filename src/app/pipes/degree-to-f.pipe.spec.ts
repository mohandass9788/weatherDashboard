import { DegreeToFPipe } from './degree-to-f.pipe';

describe('DegreeToFPipe', () => {
  it('create an instance', () => {
    const pipe = new DegreeToFPipe();
    expect(pipe).toBeTruthy();
  });
});
